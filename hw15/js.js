const vegetablesArray = ["potato", "tomato", "cucumber", "carrot", "cabbage"];

vegetablesArray.forEach((vegetable) => console.log(vegetable));

for (let vegetable of vegetablesArray) {
  console.log(vegetable);
}
